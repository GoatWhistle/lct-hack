# При дефолтных 15 параллельных сокетах npm install зависает под WSL2:
# процесс стоит в poll, тарболы не доезжают. С тремя сокетами ставится за минуты.
# Проверено 15.09.2026: один caniuse-lite качался 120 с при 15 сокетах.
maxsockets=3
fetch-retries=5
fetch-timeout=60000
audit=false
fund=false
